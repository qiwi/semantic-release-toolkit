describe('awaits', () => {
  it('https://github.com/facebook/jest/pull/11961', () => {
    expect(true).toBe(true)
  })
})

// const {createPlugin} = require('@qiwi/semrel-plugin-creator') // eslint-disable-line
//
// describe('cjs', () => {
//   it('createPlugin', () => {
//     expect(createPlugin).toEqual(expect.any(Function))
//   })
// })
