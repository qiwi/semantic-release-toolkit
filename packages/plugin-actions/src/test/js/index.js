// import def, { plugin } from '../../../target/es6'
//
// describe('export (es6)', () => {
//   it('`plugin` defined', () => {
//     expect(plugin).toEqual(expect.any(Object))
//   })
//
//   it('`default` equals `plugin`', () => {
//     expect(def).toBe(plugin)
//   })
// })

describe('', () => {
  it('', () => {})
})
