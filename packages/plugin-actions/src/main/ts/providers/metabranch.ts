/*
import { TAction } from '../interface'

export type TSyncPoint = {
  direction: 'local' | 'remote'
  branch: string
  from: string | string[]
  to: string
}

export type TSyncBatch = {
  batch: Array<TSyncPoint>
}

export interface TMetabranchPluginAction extends TAction {
  provider: 'metabranch'
  options: TSyncPoint | TSyncBatch
}
*/

export const foo = 'bar'
